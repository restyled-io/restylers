---
enabled: true
name: shellharden
version_cmd: |
  shellharden --version | sed 's/^/v/'
command:
  - shellharden
  - "--replace"
include:
  - "**/*.sh"
  - "**/*.bash"
interpreters:
  - sh
  - bash
documentation:
  - https://github.com/anordal/shellharden#readme
metadata:
  languages:
    - POSIX sh
    - Bash
  tests:
    - contents: |
        #!/bin/sh
        x=x
        var=`echo $x`
      restyled: |
        #!/bin/sh
        x=x
        var=`echo "$x"`
